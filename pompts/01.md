Read `AGENTS.md` completely before doing anything.

Implement **Phase 1 — Project Foundation** for the Cafe Management System.

Build only the project foundation:

* FastAPI application
* SQLite database
* SQLAlchemy setup
* database initialization
* static HTML/CSS/JavaScript serving
* base application layout
* sidebar navigation
* top navigation/header
* dashboard placeholder
* API error handling
* reusable frontend API utility
* reusable frontend notification/toast system
* basic responsive layout
* requirements.txt
* README.md
* .gitignore

Do not implement business modules yet.

After implementation:

1. Run the application.
2. Verify the database is created.
3. Verify FastAPI starts correctly.
4. Verify frontend pages load.
5. Verify all navigation links work, even if pages are placeholders.
6. Verify there are no console errors.
7. Add basic automated tests for application startup and database initialization.
8. Update `document.md`.
9. Update `docs/DATA_DICTIONARY.md` with currently created system tables.
10. Update `docs/API_DOCUMENTATION.md`.
11. Update the revision history.

Keep the implementation simple and suitable for a final-year academic project.

At the end, provide:

* files created
* files modified
* commands to run the project
* tests performed
* documentation updated
